name: cookie_auth_client
description: "Reusable Flutter auth client for in-memory access tokens backed by HttpOnly refresh cookies."
version: 0.2.0
publish_to: 'none'

environment:
  sdk: ^3.10.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
